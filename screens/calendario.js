import { View, Text } from 'react-native';

export default function HojariosDisponiveis() {
    return(
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Calendário será aqui</Text>
        </View>
    );
}